import React from "react";

/**
 * Native bridge is handled via CreateAccountModal.
 * No auto-login on page load - user must open Create Account and click "Continue With CarPool Account".
 */
function NativeBridgeHandler() {
  return null;
}

export default NativeBridgeHandler;
