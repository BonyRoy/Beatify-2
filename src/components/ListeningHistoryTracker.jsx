/**
 * Listening history is now recorded in PlayerContext.selectTrack (on click).
 * This component is kept for potential future use (e.g. play duration tracking).
 */
const ListeningHistoryTracker = () => null;

export default ListeningHistoryTracker;
